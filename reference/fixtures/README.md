# Frozen live-source fixtures

This directory is intentionally empty in PlaceType PH 0.1.1.

After the first successful live PSA taxonomy import, record small non-sensitive regression fixtures
here: source checksum, level histogram, selected code/parent/title rows, and the importer diagnostics
needed to detect an upstream layout change. Do not commit the full source workbook unless its terms
and repository policy explicitly allow it.
